version: "3.9"

services:
  app:
    build: .
    volumes:
      - ./interview_tg_bot:/app
    command: gunicorn django_air_erp_system.wsgi:application --bind 0.0.0.0:8000 && python main.py
    expose:
      - 8000
    env_file:
      - ./.env